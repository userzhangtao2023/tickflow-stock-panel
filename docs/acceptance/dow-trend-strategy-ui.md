# 道氏趋势策略入口验收

状态：通过（2026-07-23）

- `dow_trend` 继续作为股票内置策略显示，并使用专用多市场、多周期面板。
- 打开策略或切换市场只清空面板状态，不发送 `/api/dow-strategy/runs` 请求。
- 面板明确提示“点击‘执行选股’开始扫描”；只有用户点击按钮后才启动当前市场任务。
- 运行中显示完成数、总数和当前股票；完成后显示命中数量或空结果。
- 候选卡分别显示“局部”与“长期”命中周期。
- 行为测试先观察到旧代码自动执行的失败，再由最小实现修复；组件测试3项、路由测试1项通过。
