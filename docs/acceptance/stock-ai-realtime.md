# 个股 AI 分析盘中实时数据语义验收

- 验收对象：`SPEC-STOCK-AI-REALTIME-001`
- 产品语义：当前交易日的 AI 分析、关键价位和资金状态必须以盘中最新行情合成的当日日 K 为输入。
- 安全降级：实时行情不可用时保留最近有效落盘日 K，不伪造实时数据。
- 数据时效：接口明确返回数据日期、实时标识和行情时间戳。

## 2026-07-21 验收证据

- 定向行为测试：`backend/tests/test_stock_analysis_realtime.py` 与相关既有测试共 8 项通过。
- 生产关键价位接口：`600519.SH` 返回 `data_as_of=2026-07-21`、`is_realtime=true`、`quote_timestamp=1784617200000`、`close=1308.0`。
- 生产 AI 流首个 `meta` 事件返回相同的数据日期、实时标识、时间戳和最新价，证明 AI 与关键价位使用同一份合并后数据。
- 无实时覆盖的 `002842.SZ` 明确返回 `is_realtime=false`，保留最近有效日 K，没有伪造实时状态。
