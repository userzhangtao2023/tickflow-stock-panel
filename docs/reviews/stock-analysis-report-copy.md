# 个股分析报告复制独立需求审查

状态：2026-07-21 独立审查通过。

审查必须从 `REQ-STOCK-ANALYSIS-REPORT-COPY-001` 逐项核对按钮可见条件、Markdown 全文复制、兼容回退、反馈状态、移动端可用性、可执行测试和生产证据。快照或构建成功不能替代上述语义行为证据。

## 需求到证据核对

- 可见条件：组件继续以 `content && !isWorking` 控制入口；完成态与历史态行为测试通过，生成态隐藏测试通过。
- 复制语义：组件把未渲染的 `content` 原文传给 `copyText`；测试逐字断言含 Markdown 标记的完整正文。
- 兼容回退：工具优先 `navigator.clipboard.writeText`，失败后创建临时只读文本域并调用 `execCommand('copy')`，最终始终移除节点。
- 状态反馈：组件按工具布尔结果显示“已复制”或“复制失败”，2 秒定时恢复默认状态，并在卸载时清理定时器。
- 移动端：按钮为不可收缩的 32px 高文字按钮，无 `hidden` 断点类；标题区域手机端水平内边距由 20px 降至 12px，标题正文区域可收缩和截断。
- 自动证据：完整前端测试套件 21 个文件、55 项测试通过，TypeScript/Vite 生产构建通过，规格合规检查通过，`git diff --check` 无错误。
- 生产证据：版本化镜像 `report-copy-20260721-runtime` 运行稳定，页面返回 HTTP 200，容器实际静态资产包含新版复制操作，数据挂载未变。

## 独立结论

`REQ-STOCK-ANALYSIS-REPORT-COPY-001` 的低层复制语义、组件状态与响应式约束均有直接实现和可执行行为证据；未用构建结果或快照替代语义验收。变更不触及后端、报告生成、持久化或历史接口。
